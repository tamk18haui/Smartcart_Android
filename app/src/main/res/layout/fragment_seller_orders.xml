<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/seller_background"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingHorizontal="16dp"
        android:paddingTop="16dp"
        android:paddingBottom="8dp">

        <EditText
            android:id="@+id/edtSearchOrder"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:background="@drawable/bg_seller_input"
            android:drawableStart="@android:drawable/ic_menu_search"
            android:drawablePadding="10dp"
            android:elevation="2dp"
            android:hint="Tìm mã đơn hàng, tên khách..."
            android:imeOptions="actionSearch"
            android:inputType="text"
            android:paddingHorizontal="16dp"
            android:singleLine="true"
            android:textColor="@color/seller_text_primary"
            android:textColorHint="@color/seller_text_hint" />
    </LinearLayout>

    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clipToPadding="false"
        android:paddingHorizontal="16dp"
        android:paddingVertical="8dp"
        android:scrollbars="none">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/chipAll"
                style="@style/SellerOrderChip"
                android:layout_marginEnd="8dp"
                android:text="Tất cả đơn hàng" />

            <TextView
                android:id="@+id/chipPending"
                style="@style/SellerOrderChip"
                android:layout_marginEnd="8dp"
                android:text="Chờ xác nhận" />

            <TextView
                android:id="@+id/chipConfirmed"
                style="@style/SellerOrderChip"
                android:layout_marginEnd="8dp"
                android:text="Đã xác nhận" />

            <TextView
                android:id="@+id/chipShipping"
                style="@style/SellerOrderChip"
                android:layout_marginEnd="8dp"
                android:text="Đang giao" />

            <TextView
                android:id="@+id/chipCompleted"
                style="@style/SellerOrderChip"
                android:layout_marginEnd="8dp"
                android:text="Hoàn thành" />

            <TextView
                android:id="@+id/chipCancelled"
                style="@style/SellerOrderChip"
                android:text="Đã hủy" />
        </LinearLayout>
    </HorizontalScrollView>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvOrders"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:overScrollMode="never"
            android:paddingHorizontal="16dp"
            android:paddingTop="8dp"
            android:paddingBottom="24dp" />

        <LinearLayout
            android:id="@+id/layoutEmpty"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:orientation="vertical"
            android:visibility="gone">

            <ImageView
                android:layout_width="80dp"
                android:layout_height="80dp"
                android:alpha="0.4"
                android:src="@drawable/ic_empty_box" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:text="Không có đơn hàng ở trạng thái này"
                android:textColor="@color/seller_text_hint"
                android:textSize="15sp" />
        </LinearLayout>
    </FrameLayout>

</LinearLayout>
